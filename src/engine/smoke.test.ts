import { describe, it, expect } from 'vitest';

// Placeholder so the test runner has a target in Phase 0; replaced by the real
// engine golden tests in Phase 1.
describe('scaffold smoke', () => {
  it('runs the test runner', () => {
    expect(1 + 1).toBe(2);
  });
});
