// v2 colony-sim is the active surface (colony-sim.md). The Phase 0–6 slice components
// (outland-app etc.) remain in the repo as the prior iteration.
import './components/colony-app';
import { initServiceWorker } from './pwa';

initServiceWorker();
